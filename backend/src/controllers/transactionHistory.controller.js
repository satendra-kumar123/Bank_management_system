// TransactionController:

// depositMoney()

// withdrawMoney()

// transferMoney()

// getTransactionHistory()

const depositMoney = async(req, res) => {
    try {
        
    } catch (error) {
        console.log("error is occured", error)
        process.exit(1)
    }
}

const withdrawMoney = async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
        process.exit(1)
    }
}

const transferMoney = (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
        process.exit(1)
    }
}

const getTransactionHistory = (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
        process.exit(1)
    }
}


export { depositMoney, withdrawMoney, transferMoney, getTransactionHistory}